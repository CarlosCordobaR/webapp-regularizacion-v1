# v1.0.0 - MVP Regularizacion

Fecha: 2026-02-20

## Entregado

- RM26-031 (`Terminado`)
  - Politicas de Storage para documentos (`client-documents`).
  - Acceso por rol staff y aislamiento por `client_id` para cliente.

- RM26-032 (`Terminado`)
  - Politicas de Storage para exports (`exports`).
  - Bucket privado con control requester/staff.

- RM26-061 (`Terminado`)
  - Portal cliente dedicado (`/portal/:id`).
  - Auth de portal con token firmado.
  - Checklist expediente `missing/uploaded/accepted/rejected`.

- RM26-070 (`Terminado`)
  - Export ZIP `accepted-only`.
  - Registro de `export_jobs` con expiracion y signed URL.
  - Auditoria `EXPORT_READY`.

- RM26-072 (`Terminado`)
  - UI cliente: boton "Descargar expediente" con permisos.
  - UI staff migrada a flujo de `export_jobs`.

- RM26-092 (`Terminado`)
  - Smoke test E2E de produccion ejecutado con evidencia en Notion.

## Seguridad y calidad

- Verificacion de firma HMAC en webhook de WhatsApp (`X-Hub-Signature-256`).
- Tests backend actualizados para exportaciones, webhook y portal.
- Build frontend y compilacion backend validados.

